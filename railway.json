{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd LegalQistas && python manage.py collectstatic --no-input && python manage.py migrate && gunicorn LegalQistas.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/"
  }
}
